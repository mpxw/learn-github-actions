name: 打包React项目
on: push

permissions:
  contents: write

jobs:
  yarn-build:
    name: yarn-build工作
    runs-on: ubuntu-latest

    steps:
    - name: 读取仓库内容
      uses: actions/checkout@v5

    - name: 安装依赖和项目打包
      run: |
        yarn install
        yarn run build

    - name: 部署
      uses: JamesIves/github-pages-deploy-action@v4
      with: 
        branch: gh-pages
        folder: dist
